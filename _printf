#include "main.h"

/**
 * _printf - Printf function
 * @format: format.
 * Return: Printed chars.
 */

int _printf(const char *format, ...)
{
	int printed_chars = 0;
	va_list char_lists;

	if (format == NULL)
		return -1;
	va_start(char_lists, format);
